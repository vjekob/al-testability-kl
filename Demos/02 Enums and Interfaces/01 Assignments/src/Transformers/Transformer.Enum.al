enum 60102 Transformer implements IDog, ICar
{
    AssignmentCompatibility = true;
    AssignmentCompatibilityReason = 'Because why not?';

    value(0; Poolvo)
    {
        Implementation = IDog = Poodle, ICar = Volvo;
    }

    value(1; Huskedes)
    {
        Implementation = IDog = Husky, ICar = Mercedes;
    }

    value(2; Masla)
    {
        Implementation = IDog = Mastiff, ICar = Tesla;
    }
}
